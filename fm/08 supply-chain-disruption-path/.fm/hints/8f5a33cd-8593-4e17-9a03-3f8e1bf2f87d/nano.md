Create a clean flat-design educational infographic titled "7 Ontology Property Types & Agent Uses" with the subtitle "Supply Chain Disruption Ontology".

Layout: a single vertical 7-row matrix filling the page, read top to bottom. Above the rows, a thin sticky header bar with three column headers in small caps: "TYPE" (left, narrow), "EXAMPLE PROPERTY" (middle, wide), "AGENT USE" (right, wide). Each of the 7 rows is a rounded card split into these three aligned columns, with a small circular icon at the far left of each row and a thin right-pointing arrow separating the middle and right columns to show the mapping direction. Give each row its own accent color, applied to the type chip and the icon.

Row 1: type chip "string" (slate blue), icon = magnifying glass. Middle label "Supplier.name". Right label "Search, filter, report".
Row 2: type chip "integer" (teal), icon = gauge with warning needle. Middle label "daysOfSupplyOnHand = 3". Right label "Threshold-based alerts".
Row 3: type chip "decimal" (green), icon = coin stack with scale. Middle label "revenueAtRisk = $80M". Right label "Cost-benefit calculation".
Row 4: type chip "date" (amber), icon = calendar page. Middle label "startDate = 2024-05-01". Right label "Timeline comparison".
Row 5: type chip "datetime" (orange), icon = clock with small trend line. Middle label "assessedDate timestamp". Right label "Audit trail, trending".
Row 6: type chip "enum" (purple), icon = branching decision tree with three fixed nodes. Middle label "severity: Critical / High / Low". Right label "Classify, decision trees".
Row 7: type chip "boolean" (red), icon = toggle switch turned on with a small flag. Middle label "singleSourced = true". Right label "Risk flagging".

At the bottom, a full-width highlighted callout strip with a small shield-check icon and the text "Type = the operation an agent may trust", plus two tiny side-by-side comparison chips: a red chip labeled "free string: silent misses" and a green chip labeled "enum: safe branching".

Style: clean flat design, educational infographic, generous white space, soft off-white background, subtle rounded card shadows, muted pastel accent palette, crisp modern sans-serif typography, monospace font for property names and values, no photorealism, no clutter, high legibility. All labels short (3-5 words). Aspect ratio 4:5 portrait.
